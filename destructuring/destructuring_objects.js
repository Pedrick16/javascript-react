//// Destructuring object
const person1 = {
    name: 'Pedrick',
    age: 21,
    birthDate: new Date(),
}
const { name, age , birthDate } = person1 //need sya na match doon sa keys
console.log( name, age , birthDate)
